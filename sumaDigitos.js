// Suma de dígitos, dado un número, calcula la suma de todos sus dígitos

// Ejemplo: 1234 → 1 + 2 + 3 + 4 = 10.

let resultado = 0;

for (let i=1; i<=4; i++){

    resultado += i;

    }

console.log(resultado);